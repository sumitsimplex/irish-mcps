{
  "name": "dublin-bus-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "Dublin Bus & Bus Éireann realtime MCP server — Cloudflare Worker",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "update-routes": "node scripts/fetch-routes.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250307.0",
    "typescript": "^5.8.2",
    "wrangler": "^4.0.0"
  }
}
